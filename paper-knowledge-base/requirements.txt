chromadb>=1.5
sentence-transformers>=3.0
PyMuPDF>=1.24
tqdm>=4.66
rich>=13.7
numpy>=1.24

# PyTorch（默认安装 GPU 版，从 PyTorch 官方源拉取 CUDA 12.1 构建）
# 如果不需要 GPU，安装前移除下面两行：
#   pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0,<3.0
